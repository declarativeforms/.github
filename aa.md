After reviewing the document again, it still doesn't kind of follow an easy path for both a new user and an advanced user. And I'm thinking of completely pivoting, so I want you to use the documentation that is there as kind of a core, but make the following changes. So for the introduction section, I do want to have a Get Started or Get Started with an introduction, then a quick start, and how it works. But I want it to be around a, say, a specific example. So for example, a tenant or a rental application form. And for this introduction, it can be the simplest version of that. And you can decide what fields and what should be included. Then the next section, you can remove the concepts. I don't think that is a good approach to have. And you can replace it with a kind of a linear step-by-step. I don't know what to call the section, but where the example that is used in the quick start, it could be just an extension of that to showcase a specific feature. So for example, if it is about, say, pushing the data to the webhook, that could be a step and say how to push data to a webhook, or how to integrate. And you can come up with very marketable headings for these pages. And then the next page could perhaps be something else, and the following page could be localization. The next page could be about Mixpanel analytics. And I think that the thing that is important to focus on is it needs to consistently extend from the example. And you can do this with sections. You can do this with conditional navigation, conditional visibility on the fields, and all of these things. So first, I want you to plan out kind of a full example, and then break it down into these kind of snippets of extension. Keep in mind that there will be future developments, and I don't want you to rewrite the entire kind of step-by-step that has been built on top of the quick start. So it needs to be written in a way where if a new section gets added for a new feature, it shouldn't disrupt the flow of things and it should still make sense. 
And for these examples, when you have a page, it needs to be very focused on specific things. So if you're doing conditional navigation, it needs to stick to conditional navigation. If you're doing, say, visibility, you need to stick to the conditional visibility. If you're doing localization, it needs to be purely localization. Don't have many features overlap unless that is the purpose of the document to showcase a particular feature in context with something else. Before you start all of this, I want you to deeply analyze the project and make sure you again have a good understanding. It needs to be both the UI and the API project, and I'll also share the examples project. What I don't want you to share is the intricate detail of the API. So for example, the fact that we are doing client-side validation only should not be mentioned, as that is an implementation detail and not relevant to the consumer.

Just for reference, here is the repositories you can use

- /Users/barenderasmus/development/examples/api
- /Users/barenderasmus/development/examples/core
- /Users/barenderasmus/development/examples/examples