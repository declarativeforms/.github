---
title: Reach Anyone in Their Language
description: Translate every label, option, and message with a locale map.
---

Any string property in `rental.yaml` can be replaced with a locale map — an object keyed by locale code. The platform resolves the correct string at render time based on the visitor's language. This guide adds English and Spanish to the rental form.

---

## What to add

Replace plain strings with locale maps anywhere a string is accepted:

```yaml
title:
  en: Rental Application
  es: Solicitud de arrendamiento
locale: en
```

Apply the same pattern to field labels and option labels:

```yaml
      - id: full_name
        type: short_text
        label:
          en: Full name
          es: Nombre completo
        validators:
          - required
```

---

## How locale maps work

Both forms are valid anywhere a string is accepted. You can localize some fields and leave others as plain strings — the platform falls back to the plain string value for any locale not explicitly provided.

| Property | Where |
|----------|-------|
| `title` | Form root, section, completion |
| `description` | Form root, section |
| `label` | Fields, options |
| `placeholder` | Fields |
| `message` | Validator error messages |
| `subject` | Email connection |
| `body` | Email connection |
| `completion.title` | Form root |
| `completion.message` | Form root |

---

## Locale resolution order

The platform picks a locale using the following priority:

1. `?lang=` query parameter (e.g., `?lang=es`) — highest priority
2. Browser's `Accept-Language` header
3. The `locale` root key in the YAML
4. `en` fallback — always available

Set `locale: en` at the root to declare the default for visitors whose browser language is not explicitly matched.

---

Next: [Track Form Engagement](/guides/analytics)
