---
title: How it works
description: What happened when you pushed rental.yaml and visited the URL.
---

The quickstart gave you a live form in three steps. This page traces what the platform actually did.

---

## Where the YAML comes from

When you push `rental.yaml` to your repository's `main` branch, it becomes accessible at:

```
https://raw.githubusercontent.com/{owner}/{repo}/main/rental.yaml
```

When someone visits `https://app.declarativeforms.com/{owner}/{repo}/rental`, the API fetches that raw URL and parses the YAML. The form always reflects the current `main` branch — push a change, refresh the form URL, and the update is live.

---

## How the form loads

The browser receives the parsed form definition as JSON and renders the first section. For the quickstart `rental.yaml`, that's three fields:

- `full_name` — `short_text` renders a single-line text input
- `email` — `email` renders a text input that validates email address format before submission
- `move_in_date` — `date` renders a date picker

Field values stay in the browser until the user submits. Validators (if configured) run client-side before the section is sent to the API.

---

## How submissions work

When the user submits, the browser sends the field values to the API. The API saves a submission record and returns a `submission_id`. On the final section — which is the only section in the quickstart `rental.yaml` — the submission is marked `completed` and any configured connections fire.

---

Ready to extend the form? The Guides section adds one feature at a time, starting with [Add Multiple Steps](/guides/add-steps).
