---
title: Introduction
description: Forms are the one thing AI tools can't help you ship — until now.
---

Forms are the one thing AI tools can't help you ship. You can generate Dockerfiles, CI configs, and infrastructure as code — but a form still means opening a builder and clicking. Declarative Forms closes that gap: write YAML, push to GitHub, share a link — free, no sign-up required.

---

## How it works

1. Write a YAML file describing your form
2. Push it to a public GitHub repository
3. Share the URL: `https://app.declarativeforms.com/{owner}/{repo}/{form_name}`

---

## What it looks like

The YAML is readable enough that you can understand exactly what the form will do before it renders:

```yaml
version: 1
title: Rental Application
sections:
  - id: basic_info
    title: Basic info
    fields:
      - id: full_name
        type: short_text
        label: Full name
        validators:
          - required
      - id: email
        type: email
        label: Email address
        validators:
          - required
      - id: unit_type
        type: single_select
        label: Unit type
        options:
          - Studio
          - 1 bedroom
          - 2 bedrooms
        validators:
          - required
    next: done
```

Three fields, one section, live at a shareable URL.

---

## Why it fits how you work

**Works with AI tools.** Describe the form you need, generate the YAML, and push it directly — no clicking required.

**Lives in your repo.** Form changes are commits; you get history, diffs, and code review for free.

**No account required.** Push YAML to any public GitHub repo and your form is live instantly.

---

The quickstart walks you through it in under five minutes.

[Build your first form →](/getting-started/quickstart)

Open a [GitHub Issue](https://github.com/declarativeforms/.github/issues) with a feature request — if it genuinely improves the product, we'll ship it within 48 hours.
