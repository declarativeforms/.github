---
title: Connections
description: Integrations that push submitted data to external systems.
---

Connections define what happens with the data after a form is submitted. Each connection pushes data to an external system — a webhook endpoint, an email inbox, or an Airtable table. You define them as an array on the form.

```yaml
connections:
  - type: webhook
    url: https://example.com/hooks
  - type: email
    to: team@example.com
    subject: New submission
```

- **`type`** — The integration type. Determines how and where the data is sent.

You can attach multiple connections to the same form. Each fires independently on submission.

---

## Webhook

Posts the full submission as JSON to a URL you control.

```yaml
- type: webhook
  url: https://example.com/hooks
```

- **`type`** — Always `webhook`.
- **`url`** — The destination URL. Receives an HTTP POST with the complete submission as the JSON body.

Webhook connections fire on every submission, including partial saves from multi-section forms.

---

## Email

Sends a notification email when the form is completed.

```yaml
- type: email
  to: team@example.com
  subject: "New signup from {{data.name}}"
  body: |
    <p>You received a new signup.</p>
    <p><strong>Name:</strong> {{data.name}}</p>
    <p><strong>Email:</strong> {{data.email}}</p>
  include_responses: true
```

- **`type`** — Always `email`.
- **`to`** — The recipient email address.
- **`subject`** — The email subject line. Supports Handlebars templating — `{{data.fieldId}}` is replaced with the submitted value.
- **`body`** — Optional HTML body. Also supports Handlebars templating.
- **`include_responses`** — When `true`, appends an HTML table of all submitted field values to the email body. Defaults to `false`.

Email connections fire only on completed submissions.

---

## Airtable

Writes each completed submission as a row in an Airtable table.

```yaml
- type: airtable
  connection_id: conn_abc123
  base_id: appXXXXXXXXXXXXXX
  table_id_or_name: Signups
```

- **`type`** — Always `airtable`.
- **`connection_id`** — The OAuth connection identifier. Created through the dashboard when linking an Airtable account.
- **`base_id`** — The Airtable base ID.
- **`table_id_or_name`** — The target table, specified by ID or name.

Airtable connections fire only on completed submissions and require an OAuth connection to be set up beforehand.
