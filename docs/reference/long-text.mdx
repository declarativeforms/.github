---
title: long_text
description: Multi-line textarea field for longer free-text input.
---

```yaml
- id: bio
  type: long_text

  label: Bio                           # Required. String or localized object.
  placeholder: Tell us about yourself  # Optional. String or localized object.

  # visible_when: data.include_bio == 'Yes'  # Optional. JavaScript expression.

  validators:
    - required                         # Optional. Field must not be empty.

    - type: min                        # Optional. Minimum character count.
      value: 10
      # message: Must be at least 10 characters.  # Optional. String or localized.

    - type: max                        # Optional. Maximum character count.
      value: 2000
      # message: Must be 2000 characters or fewer.

    - type: pattern                    # Optional. Custom regex validation.
      regex: "^[\\s\\S]+$"
      # message: Invalid input.
```
