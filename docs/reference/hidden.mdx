---
title: hidden
description: Invisible field whose value is set via URL query parameter.
---

```yaml
- id: utm_source
  type: hidden
  # No label, placeholder, visible_when, or validators.
  # Value is set via URL query parameter matching the field id.
  # Example: ?utm_source=google
```
